services:
  external:
    image: fairdatasystems/sevexternal:2026-04-16
    ports: ["3000:3000"]
    env_file:
      - .env
    volumes:
      - "./data:/data"
      - "./queries-metadata:/metadata"
    environment:
      - RACK_ENV=production
      - APP_ENV=production     # both for redundancy

